extends layout

block content 
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9
        //- Set h1 to project_name.
        h1= project_name

        //- Add project description.
        p.lead.text-light
          | #{description}

      .cell.portfolio-meta.small-6.medium-2.large-3
        h6 Technologies

        //- Generate a li for each technology. 
        ul
          each technology in technologies
            li= technology
            
        //- Set the demo and github links. 
        a.button.expanded(href=`${live_link}`, target="_blank") Live Demo
        a.button.expanded(href=`${github_link}`, target="_blank") GitHub Repo
    
    //- Add three Project Landscape images. 
    img.image(src=`${image_urls[1]}`)
    img.image(src=`${image_urls[2]}`)
    img.image(src=`${image_urls[3]}`)
